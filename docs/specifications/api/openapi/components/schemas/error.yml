type: object
properties:
  status:
    type: string
    enum: [error]
    description: Response status indicating an error
    example: "error"
  error:
    $ref: "../../openapi.yml#/components/schemas/ErrorDetail"
required:
  - status
  - error
