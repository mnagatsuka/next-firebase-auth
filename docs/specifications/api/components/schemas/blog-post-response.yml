type: object
properties:
  status:
    type: string
    enum: [success, error]
    description: Response status
    example: "success"
  data:
    $ref: "./blog-post.yml"
required:
  - status
  - data
