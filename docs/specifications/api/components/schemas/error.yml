type: object
properties:
  status:
    type: string
    enum: [error]
    description: Response status indicating an error
    example: "error"
  error:
    $ref: "./error-detail.yml"
required:
  - status
  - error