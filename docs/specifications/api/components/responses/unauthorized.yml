description: Unauthorized. Authentication is required.
content:
  application/json:
    schema:
      $ref: "../schemas/error.yml"
    examples:
      default:
        $ref: "../examples/error-401.yml"
