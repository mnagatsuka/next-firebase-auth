# /specs/api/components/schemas/account-status.yml
type: object
properties:
  subscription:
    type: string
    description: The user's current subscription plan.
    example: "Pro Plan"
  renewalDate:
    type: string
    format: date
    description: The renewal date of the user's subscription.
    example: "2025-12-31"
