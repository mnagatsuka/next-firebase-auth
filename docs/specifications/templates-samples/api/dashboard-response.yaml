# /specs/api/components/schemas/dashboard-response.yml
type: object
properties:
  status:
    type: string
    enum: [success, error]
  data:
    type: object
    properties:
      accountStatus:
        $ref: "./account-status.yml" # References another schema file
      recentActivity:
        type: array
        items:
          $ref: "./activity-item.yml" # References another schema file
