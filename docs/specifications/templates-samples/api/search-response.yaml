# /specs/api/components/schemas/search-response.yml
type: object
properties:
  status:
    type: string
    enum: [success, error]
  data:
    type: object
    properties:
      results:
        type: array
        items:
          $ref: "./search-result-item.yml" # References another schema file
